//- Adds a basic password input to an html page. Not really secure, but good enough for demp projects.

script.
	function passWord() {
	var testV = 1;
	var pass1 = prompt('Please Enter Your Password',' ');
	while (testV < 3) {
	if (!pass1)
	history.go(-1);
	if (pass1.toLowerCase() == "enter") {
	alert('Right On!');
	window.open('home.html');
	break;
	}
	testV+=1;
	var pass1 =
	prompt('Access Denied - Password Incorrect, Please Try Again.','Password');
	}
	if (pass1.toLowerCase()!="password" & testV ==3)
	history.go(-1);
	return " ";
	}
center
	form
		input.demo-password(type="button", value="Enter Protected Area", onclick="passWord()")
